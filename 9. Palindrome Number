class Solution {
public:
    bool isPalindrome(int x) {
        string r=to_string(x);
        int i=0;
        int e=r.size()-1;
        while (i<e){
            if (r[i]!=r[e]){
                return false;
            }
            i++;
            e--;
        }
        return true;
    }
};
