class Solution {
public:
    int numOfStrings(vector<string>& patterns, string word) {
        int count=0;
        for (auto it:patterns){
            if (word.contains(it)){
                count++;
            }
        }
        return count;
        
    }
};
