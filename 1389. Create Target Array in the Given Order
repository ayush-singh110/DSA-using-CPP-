class Solution {
public:
    vector<int> createTargetArray(vector<int>& nums, vector<int>& index) {
        vector <int> r;
        int i=0;
        for (auto it:index){
            r.insert(r.begin()+it,nums[i]);
            i++;
        }
        return r;
        
    }
};
